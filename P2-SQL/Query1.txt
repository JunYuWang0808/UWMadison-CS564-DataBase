SELECT C_NAME, O_ORDERKEY, sum(L_QUANTITY * (L_EXTENDEDPRICE-L_DISCOUNT))
FROM ORDERS as o, LINEITEM as l, CUSTOMER as c
WHERE C_CUSTKEY = O_CUSTKEY AND L_ORDERKEY = O_ORDERKEY
GROUP BY C_NAME, O_ORDERKEY
ORDER BY sum(L_QUANTITY * (L_EXTENDEDPRICE-L_DISCOUNT)) DESC LIMIT 20
