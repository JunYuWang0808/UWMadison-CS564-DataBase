SELECT S_NAME, COUNT(DISTINCT C_NAME) as t
FROM SUPPLIER,
     LINEITEM,
     ORDERS,
     CUSTOMER
WHERE S_SUPPKEY=L_SUPPKEY
  AND L_ORDERKEY=O_ORDERKEY
  AND O_CUSTKEY=C_CUSTKEY
GROUP BY S_NAME
HAVING COUNT(DISTINCT C_NAME) > 615
ORDER BY t DESC

